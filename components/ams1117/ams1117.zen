"""AMS1117-3.3 LDO Voltage Regulator - 3-pin SOT-223."""


# -----------------------------------------------------------------------------
# IO ports
# -----------------------------------------------------------------------------

GND = io("GND", Net)
VOUT = io("VOUT", Net)
VIN = io("VIN", Net)

# -----------------------------------------------------------------------------
# Component definition
# -----------------------------------------------------------------------------

Component(
    name="AMS1117_33",
    part=builtin.Part(mpn="AMS1117-3.3", manufacturer="Advanced Monolithic Systems"),
    symbol=Symbol(library="@kicad-symbols/Regulator_Linear.kicad_sym", name="AMS1117-3.3"),
    footprint=File("@kicad-footprints/Package_TO_SOT_SMD.pretty/SOT-223-3_TabPin2.kicad_mod"),
    prefix="U",
    pin_defs={
        "GND": "1",
        "VOUT": "2",
        "VIN": "3",
        "VOUT_TAB": "4",
    },
    pins={
        "GND": GND,
        "VOUT": VOUT,
        "VIN": VIN,
        "VOUT_TAB": VOUT,
    },
    properties={
        "value": "AMS1117-3.3",
        "package": "SOT-223",
    },
)
